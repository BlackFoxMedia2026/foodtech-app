node_modules/
.next/
out/
build/
dist/

# Env
.env
.env.local
.env.development
.env.production

# Prisma
prisma/migrations/dev*

# Editor
.vscode/
.idea/
.DS_Store

# Claude Code / impeccable local state (personal, cache, locks)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.impeccable/config.local.json
.impeccable/hook.cache.json

# TypeScript incremental build cache
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Coverage
coverage/
.nyc_output/

# Next types
next-env.d.ts
.vercel
.env*
*.tsbuildinfo

# Percorsi end-to-end: esiti, tracce e sessione di prova non si versionano
/test-results/
/tests/e2e/.auth/
/tests/e2e/.report/

# Tavole di Claude Design: materiale di lavoro, non prodotto
.design-*/

# Archivio file su disco: il ripiego di `server/archivio-file.ts` quando manca
# BLOB_READ_WRITE_TOKEN. Sono i file caricati in locale, non sorgente.
.archivio-locale/

# Cartelle di build di un secondo `next dev` (vedi NEXT_DIST_DIR in next.config.mjs)
.next-*
